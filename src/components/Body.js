import React from "react";

const Body = () => {
  return (
    <div className="min-h-screen flex items-center justify-center bg-gradient-to-r from-blue-500 to-purple-600 text-white">
      <p className="text-4xl">🚀 Tailwind + Webpack + React Works!</p>
    </div>
  );
};

export default Body;
